// import { useContext } from "react"
// import { DataContext } from "../util/context"

export const IngredientsView = () => {
  // const testContext = useContext(DataContext).ingredientsList || "nothing here"

  return(
    <>
      IngrediensView 
    </>
  )
}